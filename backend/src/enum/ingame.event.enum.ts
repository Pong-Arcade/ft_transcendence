export enum InGameKeyEvent {
  KEY_UP = -1,
  KEY_DOWN = 1,
  STOP = 0,
}

export enum InGamePlayer {
  RED = 'RED',
  BLUE = 'BLUE',
}

export enum InGameEvent {
  CONFIG = 'setConfig',
  UPDATE = 'update',
  SCORE = 'score',
  FINISH = 'finish',
}

export enum InGameKeyEvent {
  ARROW_UP = -1,
  ARROW_DOWN = 1,
  STOP = 0,
}

export enum InGamePlayer {
  RED = 'RED',
  BLUE = 'BLUE',
}

export enum InGameEvent {
  CONFIG = 'config',
  UPDATE = 'update',
  SCORE = 'score',
  FINISH = 'finish',
}

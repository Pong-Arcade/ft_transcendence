import React from "react";
import { redirect, useOutletContext } from "react-router-dom";

const Login = () => {
  // const [isLogin, setIsLogin] = useOutletContext();
  // if (isLogin) redirect("/lobby");
  // setIsLogin(true);
  return <div>Login</div>;
};

export default Login;

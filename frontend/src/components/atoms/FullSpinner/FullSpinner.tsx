import ModalWrapper from "../ModalWrapper";
import Spinner from "../Spinner";

const FullSpinner = () => {
  return (
    <ModalWrapper>
      <Spinner />
    </ModalWrapper>
  );
};

export default FullSpinner;
